<!-- UX prototype: journey-style rewrite of writing-assistant/.nightwatch/RELEASE.md using the
real 2026-07-10/11 tracker data. Written under the draft primary objective (see
objectives-and-prior-art.md): "the maintainer can state the release goal, their current
milestone, and the next milestone within 1 minute of opening it." Exercises findings 0021
(journey not status report) and 0022 (status entries answer 'what changed, does it need
you?'). Frontmatter, stable ids, byte-preserved Notes, and the single-writer contract are
unchanged — this prototypes the reader-facing composition only. Not a spec. -->

---
phase: hardening
target: "First release — all planned epics complete"
progress: 0.67
updated: 2026-07-11
---
# Release progress — the road to *First release*

## The road

**Goal (yours, declared in STATE.md):** all planned epics complete, the writing pipeline
proven end-to-end, and the install/usage docs validated.

- ✓ **1. All planned epics complete** — 8 epics / 40 stories merged (re-confirmed
  2026-07-11 after Epics 7–8 landed; the plan has grown from 6 epics since first tracked)
- ✓ **2. Writing pipeline works end-to-end** — harvest → draft-article → review-article,
  first dogfood cycle passed 2026-07-10
- ▶ **3. Install & usage docs validated** — *current milestone.* Two gaps left:
  - [ ] README asset-paths line uses the wrong variable — hand-fix `README.md:155`
    (`${CLAUDE_SKILL_DIR}` → `${CLAUDE_PLUGIN_ROOT}`) · RC-615fba
  - [ ] Config-validation friction from the dogfood run — settle what
    `docs/dogfood-findings.md` (2026-07-10) recorded
- ○ **Hygiene gate before tagging** *(generic checks, not your declaration — waivable)*:
  - [ ] CI workflow (`.github/workflows/` absent) · RP-70609d
  - [ ] CHANGELOG (absent; seed it from the Epic 7/8 merges) · RP-09c718
- 🏁 **Tag the first release.**

**Blocked by:** nothing. No severity-1 findings, no decisions waiting on you.

## Next actions (top 3)

1. Fix `README.md:155` — 2 minutes; finishes half of milestone 3 (clears RC-615fba).
2. Settle the config-validation friction — closes milestone 3 and your definition of done.
3. Add CHANGELOG + CI (or explicitly waive the hygiene gate) — then tag.

## What changed lately (latest first, capped)

- 2026-07-11 — **No forward movement, one regression to know about:** RC-615fba is still
  unfixed and its ready-made patch was lost in the forced re-run — the fix is now a
  2-minute hand edit (`README.md:155`). Everything else is unchanged; nothing blocks you.
- 2026-07-10 — **Tracker started at 2 of 3 milestones done.** Epics and the dogfood
  cycle were already complete; docs validation opened as the current milestone (README
  drift + config friction). Hygiene gaps noted: no CI, no CHANGELOG.

## Done — evidence appendix

- Milestone 1: `_bmad-output/implementation-artifacts/*.md` (40 stories), Epic 7/8 merges
  #78–#85 (2026-07-10); originally recorded at 6 epics/29 stories, `epics.md:96`.
- Milestone 2: `skills/*/SKILL.md` exercised; `docs/dogfood-findings.md:13` (review-article
  run completed, draft publishable).
- Generic passes: LICENSE (`LICENSE:1`), README Install/Usage sections (`README.md:8,82`),
  no secrets, TODO count 0/≤40 — `release-checks-2026-07-10.json`.

## Parked (nice to have)

- Arch-review candidates AR-244928 (scripts↔skills coupling) and AR-7c1754 (`.gitignore`
  hotspot) — refuted twice, revisit only if corroborated.
- `version_tag` check skipped — no `package.json` version to compare against tags.

## Phase

hardening — *mirrors STATE.md `phase`.*

## Notes (human-owned — never machine-edited)
<!-- Anything below this heading is byte-preserved by Nightwatch. Write freely. -->
