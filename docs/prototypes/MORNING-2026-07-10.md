<!-- UX prototype: a rewrite of writing-assistant/.nightwatch/MORNING.md (2026-07-10), same
underlying data. Single goal: an exhausted maintainer knows the first concrete action within
30 seconds. Companion to dogfooding findings 0016/0017. Not a spec, not an implementation
proposal. Read it cold, in the morning, and time yourself. -->

# Nightwatch — 2026-07-10

**Quiet night.** Nothing is blocking, no decisions needed, nothing broke.
One ready-made fix is waiting for you.

## ▶ First action

- [ ] **Apply the ready-made README fix** — 2 min, one command:

      git apply .nightwatch/out/reconcile-2026-07-10.patch

  The README's "Asset paths" section names the wrong variable (docs say `${CLAUDE_SKILL_DIR}`,
  the code uses `${CLAUDE_PLUGIN_ROOT}`). The patch corrects the docs. → [details A](#a)

## If you have energy after that

- [ ] **Tell Nightwatch about 3 new folders** — 2 min: run `/nightwatch init --update`
  (or add `.claude-plugin/`, `config/`, `skills/` to `config.yaml`). Until you do, they are
  invisible to review. → [details B](#b)
- [ ] **Start a CI workflow** — the biggest remaining release gap; no time pressure. → [details C](#c)

## Where you stand

**2 of 3 release criteria met (67%)** toward *First release — all planned epics complete*.
The one criterion left is "usage docs validated" — the README fix above advances it.
After that, only generic hygiene remains (CI, CHANGELOG). Full tracker: `.nightwatch/RELEASE.md`.

---

*Everything below is supporting detail. You can stop reading here.*

## Details

### A — README asset-paths drift <a id="a"></a>

`README.md:133` says skills reference their bundled files (including `scripts/`) via
`${CLAUDE_SKILL_DIR}`. In reality every `SKILL.md` uses `${CLAUDE_PLUGIN_ROOT}`, and the
repo's own convention assigns shared `scripts/` to `${CLAUDE_PLUGIN_ROOT}`. The README is
declared *derived* (it follows the code), so this is mechanically fixable — hence the patch.

- Evidence: `README.md:133`; `skills/draft-article/SKILL.md:20,29`; `skills/harvest/SKILL.md:25`;
  `skills/review-article/SKILL.md:94`; `scripts/check-dev-harness.sh:96`
- Severity 2 · id `RC-615fba` · patch: `.nightwatch/out/reconcile-2026-07-10.patch`

### B — Three new top-level directories are unclassified <a id="b"></a>

`.claude-plugin/`, `config/`, and `skills/` appeared since config was last confirmed. Nightwatch
never guesses scope, so until they are classified (analyzed vs dev-tooling vs ignored) they are
excluded from review. One `/nightwatch init --update` classifies all three interactively.

### C — Remaining release checks <a id="c"></a>

- **CI** — no `.github/workflows/` found (`ci_present` = fail) · id `RP-70609d`
- **CHANGELOG** — none present (`changelog` = fail) · id `RP-09c718`
- Also on the docs criterion: a config-validation friction from the 2026-07-10 dogfood run is
  recorded in `docs/dogfood-findings.md` — settle it together with the README fix.

## Machine notes — nothing to act on

- repo-reconcile ran degraded: no extractor for ecosystem "unknown"; universal fallback only
  (file tree, command files, README claims).
- repo-reconcile: `specs/*.md` were modified by another process during the run. Tonight's
  finding's evidence lies outside the modified set, so it is unaffected.
- arch-review: layering checks skipped — no `layers:` declared in config (not an error).
- Scope: `.claude`, `.nightwatch`, `_bmad`, `_bmad-output`, `q_a` excluded (ignore +
  dev_tooling) — edit `.nightwatch/config.yaml` to change.
- Architecture review: 0 verified findings this week.

---

*Mark boxes `[x]` (done) or `[-]` (not doing) — the next run reads them back.
4 findings total, 4 shown, cap 25. Or review interactively: `/nightwatch review`.*
