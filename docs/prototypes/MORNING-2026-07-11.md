<!-- UX prototype v2: roadmap-first rewrite of writing-assistant/.nightwatch/MORNING.md using
the real 2026-07-10/11 data. Written under the draft primary objective (see
objectives-and-prior-art.md): "the maintainer begins productive work within 3 minutes of
opening it." Exercises findings 0018 (roadmap), 0020 (finished/now/next + Details as work
narrative), 0022/0023 (every block opens with its answer). Composition: status → since
yesterday → road → first action — the 0018 synthesis where the fire-check stays first and
the first action is a step on the road. Not a spec. Read it cold and time yourself. -->

# Nightwatch — 2026-07-11

**Quiet night. Nothing is blocking.** You're one small milestone from your release goal; the first step takes 2 minutes.

## Since yesterday — what you finished

- **Epics 7 and 8 fully merged** (8 PRs, #78–#85): stage-0 config validation, completion
  summaries, route-config defect handling, and the whole article-visuals pipeline (slots →
  proposals → source-pointing → fallback ladder → platform variants).
- That closes **every planned epic — 8 epics / 40 stories** (the plan grew from 6 epics
  since the goal was first tracked; the roadmap tick below was re-confirmed against the
  grown plan).

## The road to release

**Your goal (STATE.md):** *First release — all planned epics complete.*

- ✓ **1. All planned epics complete** — re-confirmed at 8 epics / 40 stories after
  yesterday's merges
- ✓ **2. The writing pipeline works end-to-end** — harvest → draft-article →
  review-article, first dogfood cycle passed
- ▶ **3. Install & usage docs validated** ← *you are here.* Two small gaps left, both
  below.
- ○ **Hygiene gate before tagging** (generic checks, not your definition of done): CI
  and CHANGELOG are still missing.

**Blocking the release:** nothing.

## ▶ First action — 2 min, finishes half of milestone 3

- [ ] **Fix the README asset-paths line**: at `README.md:155`, change
  `${CLAUDE_SKILL_DIR}` → `${CLAUDE_PLUGIN_ROOT}`. <!-- ids: RC-615fba -->
  The README claims a variable no skill uses; this is the drift found on 07-10 — its
  ready-made patch was lost in the forced re-run, so it's a hand edit now. → [details](#d-rc)

## If you have energy after that

- [ ] **Settle the config-validation friction** noted in `docs/dogfood-findings.md` —
  the second half of milestone 3; validating it closes your last DoD item. <!-- ids: DOD-3 -->
- [ ] **Add `CHANGELOG.md`**, seeded from yesterday's Epic 7/8 merges — half the hygiene
  gate. <!-- ids: RP-09c718 -->
- [ ] **Add a CI workflow** under `.github/workflows/` — the other half. <!-- ids: RP-70609d -->

---
*Everything below is supporting detail. You can stop reading here.*

## Details

### Where the work stands (context for continuing)

Milestone 3 is the only DoD item open. Its two gaps are independent and small: the README
variable fix (above) and the config-validation friction your 07-10 dogfood run recorded.
When both land, your declared definition of done is met — what remains before tagging is
only the generic hygiene gate (CI, CHANGELOG), which you may also waive: it's Nightwatch's
generic checklist, not your declaration.

Upcoming after release: nothing is queued. The arch-review candidates (scripts↔skills
coupling, `.gitignore` hotspot) were refuted again tonight and stay parked unless they
recur with corroboration.

### Finding appendix <a id="d-rc"></a>

- `RC-615fba` (sev 2, drift): README.md:155 attributes bundled `scripts/` to
  `${CLAUDE_SKILL_DIR}`; every SKILL.md and `scripts/check-dev-harness.sh:96` use
  `${CLAUDE_PLUGIN_ROOT}`. README is declared derived → mechanically fixable. Patch no
  longer on disk (07-11 forced re-run overwrote `out/`).
- `RP-09c718` / `RP-70609d` (sev 3, hygiene): CHANGELOG and CI absent —
  `release-checks-2026-07-10.json`.

## Machine notes — nothing to act on

- repo-reconcile: degraded — no extractor for ecosystem "unknown"; universal fallback only.
- arch-review: degraded — no `layers:` declared; layering checks skipped. 0 verified findings.
- 3 top-level directories are unclassified (`.claude-plugin/`, `config/`, `skills/`) — one
  `/nightwatch init --update` classifies them and also installs the `.nightwatch/README.md`
  orientation file this install predates.
- Scope: excluded .claude, .devcontainer, .nightwatch, _bmad, _bmad-output, q_a — edit
  `.nightwatch/config.yaml` to change.

---
_Mark boxes `[x]` done / `[-]` not doing — the next run reads them back. Or review
interactively: `/nightwatch review`._
